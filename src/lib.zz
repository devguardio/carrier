using <stdio.h>::{printf};
using crc8;
using err;
using identity;
using cipher;
using sha256;
using hpack::encoder;
using hpack::decoder;
using rand;

export fn hellozz() -> int {
    printf("hello carrier\n");
    return 0;
}
