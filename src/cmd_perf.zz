using initiator;
using err;
using vault;
using string;

pub fn cmd(vault::Vault mut *va, usize mut argc, char ** argv) -> int
    where (usize)argc == len(argv)
{

    new+1200 pkt  = string::make();
    new+100 e = err::make();

    for (usize mut  i = 0 ; i < 1000; i++) {
        new ii = initiator::initiate(&e, false , va, initiator::Move::Self, 0, 0, 0, &pkt);
    }
    return 0;
}
